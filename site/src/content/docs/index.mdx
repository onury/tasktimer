---
title: TaskTimer
description: An accurate timer utility for running periodic tasks on the given interval ticks or dates.
template: splash
hero:
  tagline: An accurate timer utility for running periodic tasks on the given interval ticks or dates — one timer, many tasks, with drift correction.
  actions:
    - text: Get Started
      link: /tasktimer/getting-started/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/onury/tasktimer
      icon: external
      variant: minimal
---

```js
const timer = new TaskTimer(1000);          // 1s base interval
timer.add(task => console.log(task.currentRuns)).start();
```

{/* Feature cards use the shared `.feature-grid` classes from @onury/docs-kit. */}
<div class="feature-grid">
  <div class="feature">
    <div class="feature-head">
      <i class="ti ti-target-arrow feature-icon fi-purple"></i>
      <h3>Accurate</h3>
    </div>
    <p>Drift between ticks is auto-corrected against task/CPU load and clock drift — using high-resolution time in Node.</p>
  </div>
  <div class="feature">
    <div class="feature-head">
      <i class="ti ti-stack-2 feature-icon fi-blue"></i>
      <h3>Many Tasks, One Timer</h3>
    </div>
    <p>Run and schedule any number of tasks — each on its own interval, delay, run limit, or date window — from a single instance.</p>
  </div>
  <div class="feature">
    <div class="feature-head">
      <i class="ti ti-refresh feature-icon fi-green"></i>
      <h3>Sync or Async</h3>
    </div>
    <p>Return a <code>Promise</code> or call <code>done()</code>. Limit runs, listen for events, pause and resume at any time.</p>
  </div>
  <div class="feature">
    <div class="feature-head">
      <i class="ti ti-package feature-icon fi-orange"></i>
      <h3>ESM, Zero-Dependency</h3>
    </div>
    <p>An ESM-only module with no runtime dependencies, written in TypeScript with complete type definitions.</p>
  </div>
</div>

<p class="site-copyright">© {new Date().getFullYear()}, <a href="https://github.com/onury">@onury</a>&nbsp;&nbsp;&nbsp;•&nbsp;&nbsp;&nbsp;<a href="https://github.com/onury/tasktimer/blob/HEAD/LICENSE">MIT License</a></p>
